const { QueryType } = require("discord-player");
const {MessageEmbed} = require("discord.js")

module.exports = {
    name: "loop",
    description: "shows the loop menu!",
    permissions: "NONE",
    onlyguild: true,
    run: async(client, interaction, args) =>{
        
    }
} 